//* ciclo for
// Empezamos con un contador en 0.
// Mientras el contador sea menor o igual a 5, el bucle se ejecutará.
// En cada iteración, aumentamos el contador en 1.
// Imprimimos el valor actual del contador en la consola.
for (let contador = 0; contador <= 5; contador++) {
    console.log(contador);
}
